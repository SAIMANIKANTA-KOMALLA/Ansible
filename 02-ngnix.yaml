- name: ngnix install and run
  host: frontend
  tasks:
  - name : install ngnix server
    ansible.builtin.package:
      name: ngnix
      state : present

  - name : run ansible server 
    ansible.builtin.service:
      name : ngnix
      state : started
      enabled : yes
